export enum IdentityProvider {
  ENTRA_ID = "entra_id",
  AUTH0 = "auth0",
  IDENTITY_SERVER = "identity_server",
  KEYCLOAK = "keycloak",
  OKTA = "okta",
  COGNITO = "cognito",
  FIREBASE_AUTH = "firebase_auth",
  CUSTOM = "custom",
  NONE = "none"
}
